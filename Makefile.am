AUTOMAKE_OPTIONS = no-dependencies dist-shar dist-zip
EXTRA_DIST = reconfig README *.pc.in *.spec.in *.spec

DIST_SUBDIRS = src inc test
SUBDIRS = src inc

pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = ucommon.pc


